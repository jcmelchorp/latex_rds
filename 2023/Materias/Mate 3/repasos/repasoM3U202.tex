\documentclass[12pt,addpoints]{repaso}
\grado{3}
\nivel{Secundaria}
\cicloescolar{2023-2024}
\materia{Matemáticas}
\unidad{2}
\title{Practica la Unidad}
\aprendizajes{
        \item 
}
\author{Melchor Pinto, J.C.}
\begin{document}
\INFO%
\ejemplosboxed[\include*{../questions/question025a}]
\begin{questions}
    % \section*{\ifprintanswers   {Probabilidad y estadística}\else{}\fi}
    % \subsection*{\ifprintanswers{Mediana y moda}\else{}\fi}
    % \subsection*{\ifprintanswers{Promedio}\else{}\fi}
    % \subsection*{\ifprintanswers{Desviación media}\else{}\fi}
    % \subsection*{\ifprintanswers{Eventos mutuamente excluyentes}\else{}\fi}
    % \subsection*{\ifprintanswers{Eventos dependientes e independientes}\else{}\fi}
    % \section*{\ifprintanswers{   Figuras y cuerpos geométricos}\else{}\fi}
    % \subsection*{\ifprintanswers{Perímetro}\else{}\fi}
    % \subsection*{\ifprintanswers{Área}\else{}\fi}
    % \subsection*{\ifprintanswers{Área lateral y total}\else{}\fi}
    % \subsection*{\ifprintanswers{Volumen}\else{}\fi}
    % \subsection*{\ifprintanswers{Resolución de problemas}\else{}\fi}
    % \section*{\ifprintanswers{   Plano cartesiano y recta}\else{}\fi}
    % \subsection*{\ifprintanswers{Ubicación en el plano cartesiano}\else{}\fi}
    % \subsection*{\ifprintanswers{Cuadrantes en el plano cartesiano}\else{}\fi}
    % \subsection*{\ifprintanswers{Pendiente y ordenada}\else{}\fi}
    % \subsection*{\ifprintanswers{Pendiente dados dos puntos}\else{}\fi}
    % \subsection*{\ifprintanswers{Ecuación de una recta}\else{}\fi}
    % \section*{\ifprintanswers{   Ecuación lineal}\else{}\fi}
    % \subsection*{\ifprintanswers{Lenguaje algebraico}\else{}\fi}
    % \subsection*{\ifprintanswers{Ecuaciones lineales 1}\else{}\fi}
    % \subsection*{\ifprintanswers{Ecuaciones lineales 2}\else{}\fi}
    % \subsection*{\ifprintanswers{Ecuaciones lineales con fracciones}\else{}\fi}
    % \subsection*{\ifprintanswers{Resolución de problemas}\else{}\fi}
    % \section*{\ifprintanswers{   Sistemas de ecuaciones}\else{}\fi}
    % \subsection*{\ifprintanswers{Método de eliminación}\else{}\fi}
    % \subsection*{\ifprintanswers{Método de sustitución e igualación}\else{}\fi}
    % \subsection*{\ifprintanswers{Sistema de ecuaciones 3x3}\else{}\fi}
    % \subsection*{\ifprintanswers{Sistema de ecuaciones con fracciones}\else{}\fi}
    % \subsection*{\ifprintanswers{Resolución de problemas}\else{}\fi}
    \question[10]
\end{questions}
\end{document}